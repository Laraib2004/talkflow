# Virtualenvs (per-machine — never commit; see README portability note)
.venv/
venv/
env/

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Local model cache (downloaded per machine)
*.gguf

# OS / editor cruft
.DS_Store
Thumbs.db
